*requires autohotkey v2
*application paths are set to default install spots for app downloads
to run these scripts just double click them and they shoud run in back ground
made for windows 11
to launch them on start up 
1. make a shortcut for each script
2. windows key + r
3. run shell:startup
4. drag the shortcut to the folder that opens (.\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup)
5. the script should launch automaticaly

app hotkeys:

discord ctrl + alt + d

spotify ctrl + alt + s

google chrome ctrl + alt + g

minimise everything ctrl + alt + m


resolution hotkeys:

1920 x 1080 ctrl + alt + 1

1440 x 1080 ctrl + alt + 2

800 x 900 ctrl + alt + 3
